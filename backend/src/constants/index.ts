import cookie from './cookie';
import errors from './errors';
import info_messages from './infoMessages';
import regex from './regex';

export default {
  COOKIE: cookie,
  ERROR_CODE: errors,
  VALID_MSG: info_messages,
  REGEX: regex,
};
